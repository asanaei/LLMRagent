% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{agent_workflow}
\alias{agent_workflow}
\title{Build an agent workflow (a small, explicit graph)}
\usage{
agent_workflow(name)
}
\arguments{
\item{name}{A label for the workflow.}
}
\value{
An \code{agent_workflow} object (built up immutably by \code{\link[=add_node]{add_node()}} /
\code{\link[=add_edge]{add_edge()}}).
}
\description{
A workflow is a directed graph whose nodes transform a shared, explicit
\code{state} list. Build it with \code{agent_workflow()} then \code{\link[=add_node]{add_node()}} and
\code{\link[=add_edge]{add_edge()}}; run it with \code{\link[=run_workflow]{run_workflow()}}. The runtime is minimal:
sequential execution, one mutable state, and a checkpoint after each node. A
run is therefore auditable and resumable. Reach for it only when a study genuinely
needs branching, looping, or mid-run human review; \code{\link[=agent_pipeline]{agent_pipeline()}} and
\code{\link[=conversation]{conversation()}} remain the simpler interfaces.
}
\examples{
wf <- agent_workflow("classify") |>
  add_node("clean", function(state) { state$x <- trimws(state$input); state }) |>
  add_node("label", function(state) { state$label <- nchar(state$x) > 3; state }) |>
  add_edge("clean", "label")
run <- run_workflow(wf, input = "  hello  ")
run$state$label
}
\seealso{
\code{\link[=add_node]{add_node()}}, \code{\link[=add_edge]{add_edge()}}, \code{\link[=run_workflow]{run_workflow()}}, \code{\link[=resume_workflow]{resume_workflow()}},
\code{\link[=fork_workflow]{fork_workflow()}}, \code{\link[=replay_run]{replay_run()}}
}
