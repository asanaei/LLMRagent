% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persona.R
\name{persona_frame}
\alias{persona_frame}
\alias{print.persona_frame}
\alias{as.character.persona_frame}
\title{A persona as an auditable research object}
\usage{
persona_frame(
  text,
  source = NULL,
  scope = NULL,
  attributes = NULL,
  variant_of = NULL,
  id = NULL
)

\method{print}{persona_frame}(x, ...)

\method{as.character}{persona_frame}(x, ...)
}
\arguments{
\item{text}{The persona brief (character scalar): who this person is, what
they want, how they speak. The only field the model sees.}

\item{source}{Where the brief came from, e.g. \code{"synthetic"},
\code{"interview-grounded"}, \code{"literature"}, or \code{NULL} if unrecorded.}

\item{scope}{Optional named list of scope conditions the persona is meant to
hold under (e.g. \code{list(country = "US", year = 2024)}); recorded as
provenance, not enforced.}

\item{attributes}{Optional named list of the dimensions varied to produce
this brief (e.g. \code{list(age = "52", risk = "cautious")}).}

\item{variant_of}{Optional parent persona hash this frame was derived from.}

\item{id}{Optional explicit identifier; ignored for hashing (the \code{hash} is
always content-derived) but kept on the object when supplied.}

\item{x}{A \code{persona_frame}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{persona_frame}: a list with \code{text}, \code{source},
\code{scope}, \code{attributes}, \code{variant_of}, \code{id}, and \code{hash}.
}
\description{
A \code{persona_frame} bundles the brief a model reads (\code{text}, the \emph{only} thing
the model sees) with the provenance that makes a synthetic persona
inspectable: its \code{source}, the \code{scope} conditions it is meant to hold under,
the \code{attributes} that were varied to produce it, an optional \code{variant_of}
parent hash, and a stable content \code{hash} (via \code{\link[=hash_persona]{hash_persona()}}). A frame is a
drop-in replacement for a plain-string persona anywhere \code{\link[=agent]{agent()}} accepts
one: \code{\link[=as.character]{as.character()}} returns its \code{text}, and the \link{Agent} reads \code{text}
directly while keeping the frame for provenance.
}
\details{
Provenance, not authority: a persona is a prompt with a provenance record, never a
claim that the model speaks for the people it sketches. Pair with
\code{\link[=persona_audit]{persona_audit()}} before trusting a brief, and with \code{\link[=persona_variants]{persona_variants()}} to
turn one frame into a designed set.
}
\examples{
p <- persona_frame(
  "A retired schoolteacher who reads the local paper and distrusts polls.",
  source = "synthetic",
  scope = list(country = "US"))
print(p)
as.character(p)            # the brief, usable wherever a string persona is
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b")
a <- agent("Voter", cfg, persona = p)   # the frame is accepted directly
a$persona_frame()$hash                  # provenance is preserved
}
}
\seealso{
\code{\link[=persona_variants]{persona_variants()}}, \code{\link[=persona_audit]{persona_audit()}}, \code{\link[=hash_persona]{hash_persona()}}, \code{\link[=agent]{agent()}}
}
