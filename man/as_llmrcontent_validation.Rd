% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{as_llmrcontent_validation}
\alias{as_llmrcontent_validation}
\title{Build a validation frame for LLMRcontent}
\usage{
as_llmrcontent_validation(predictions, gold, id = NULL)
}
\arguments{
\item{predictions}{The model's predictions on the labeled units (a vector),
or a tibble with a single prediction column.}

\item{gold}{The human labels on the same units, aligned to \code{predictions}.}

\item{id}{Optional ids for the units; defaults to a 1..n sequence.}
}
\value{
A tibble with columns \code{id}, \code{prediction}, \code{gold}.
}
\description{
Returns a tidy frame shaped for an external validator (the \code{LLMRcontent}
package's content-validation entry point): one row per labeled unit with the
aligned \code{id}, \code{prediction}, and \code{gold}. This is the hand-off frame; it does
not call \code{LLMRcontent}. Pass the result to \code{LLMRcontent}'s validator (for
example \code{LLMRcontent::validate_against_gold(frame)}) for the heavier
diagnostics (confusion matrix, per-class precision/recall, calibration
curves) that are out of scope for this thin local estimator.
}
\examples{
frame <- as_llmrcontent_validation(
  predictions = c("pos", "neg", "pos"),
  gold        = c("pos", "neg", "neg"))
frame
}
\seealso{
\code{\link[=agent_calibrate]{agent_calibrate()}}
}
