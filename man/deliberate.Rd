% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/presets.R
\name{deliberate}
\alias{deliberate}
\title{Group deliberation with a recorded vote}
\usage{
deliberate(
  agents,
  proposal,
  rounds = 2L,
  options = c("yes", "no", "abstain"),
  msg_mode = NULL,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{agents}{A list of \link{Agent}s.}

\item{proposal}{The proposal under deliberation (character scalar).}

\item{rounds}{Discussion rounds before the vote (default 2).}

\item{options}{Vote options. Default \code{c("yes", "no", "abstain")}.}

\item{msg_mode}{Message construction, \code{"roleflip"} (default) or \code{"flat"};
\code{NULL} uses \code{getOption("LLMRagent.msg_mode")}. See \code{\link[=conversation]{conversation()}}.}

\item{quiet}{FALSE prints the deliberation live.}

\item{...}{Passed to the underlying LLMR calls.}
}
\value{
An object of class \code{agent_deliberation}: a list with \code{transcript}
(tibble: \code{turn}, \code{round}, \code{speaker}, \code{text}), \code{votes} (tibble: \code{voter},
\code{vote}, \code{reason}), \code{tally} (table), \code{decision} (modal vote, NA on ties),
and \code{proposal}. \code{as.data.frame()} returns the transcript.
}
\description{
Agents discuss a proposal for a fixed number of rounds (everyone speaks
each round, seeing the discussion so far), then vote independently and
privately through structured output. The tidy return supports comparing
votes cast after deliberation with positions voiced during it.
}
\examples{
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0.9)
panel <- list(
  agent("Aila", cfg, persona = "Data-driven, cautious about side effects."),
  agent("Bo",   cfg, persona = "Mission-driven, impatient with delay."),
  agent("Cyn",  cfg, persona = "A budget hawk.")
)
d <- deliberate(panel, "Adopt a four-day work week for one pilot year.")
d$tally; d$decision
}
}
