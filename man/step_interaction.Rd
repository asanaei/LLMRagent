% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/society.R
\name{step_interaction}
\alias{step_interaction}
\title{Advance one interaction round}
\usage{
step_interaction(society, who = NULL, prompt = NULL, ...)
}
\arguments{
\item{society}{A \code{\link[=society]{society()}}.}

\item{who}{Optional character vector of agent names to speak this round; the
default lets every connected agent speak.}

\item{prompt}{Optional instruction appended as the "your turn" cue for each
speaker. Default: "Contribute to the discussion."}

\item{...}{Passed to each agent's underlying LLMR call.}
}
\value{
The updated \code{\link[=society]{society()}} (its \code{step} incremented and \code{history}
extended by one utterance per speaker).
}
\description{
Run a single round of the society: the chosen agents each speak once, given
the shared \code{history} so far, and their utterances are appended with a new
step index. Speaking uses the \link{Agent}'s stateless \code{reply()} over the shared
transcript (the same role-flipped construction \code{\link[=conversation]{conversation()}} uses), so no
agent writes to its own memory and the transcript remains the single record.
}
\details{
Who speaks: all of \code{who} (by agent name) when supplied, otherwise every agent
that has at least one edge in the network (an isolate with no edges is
skipped). Each speaker currently sees the full shared history; the edge list
is recorded as the exposure structure (see \code{\link[=exposure_matrix]{exposure_matrix()}}) rather than
used to mask the transcript, which keeps the round simple while leaving the
connectivity available for analysis.
}
\examples{
\dontrun{
soc <- step_interaction(soc, prompt = "React in one sentence.")
soc$history
}
}
\seealso{
\code{\link[=society]{society()}}, \code{\link[=collect_measures]{collect_measures()}}, \code{\link[=exposure_matrix]{exposure_matrix()}}
}
