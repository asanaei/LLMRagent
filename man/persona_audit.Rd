% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persona.R
\name{persona_audit}
\alias{persona_audit}
\alias{print.persona_audit}
\title{Audit persona briefs for essentializing language and caricature}
\usage{
persona_audit(p_or_set, config = NULL, dimensions = NULL)

\method{print}{persona_audit}(x, ...)
}
\arguments{
\item{p_or_set}{A \code{\link[=persona_frame]{persona_frame()}}, a \code{\link[=persona_variants]{persona_variants()}} result
(\code{persona_set}), or a list of \code{persona_frame} objects.}

\item{config}{Optional generative \code{LLMR::llm_config()} for model scoring.
When \code{NULL} (default), only the lexical layer runs and model scores are
\code{NA}.}

\item{dimensions}{Optional character vector naming the qualities to score
(model layer); defaults to caricature, out-group homogeneity, and
essentialism. Recorded in the judge prompt.}

\item{x}{A \code{persona_audit}.}

\item{...}{Ignored.}
}
\value{
A tibble of class \code{persona_audit}, one row per persona, with columns
\code{id}, \code{flag_lexical} (any lexical hit), \code{n_lexical_hits}, \code{caricature_score}
(0--1 or \code{NA}), \code{essentialism_score} (0--1 or \code{NA}), and \code{notes}.
}
\description{
Read persona briefs back as text and flag the ways synthetic personas fail
representationally. Two layers:
}
\details{
\itemize{
\item \strong{Lexical} (always, no model): each brief is scanned against a small
built-in lexicon of essentializing and demographic-as-destiny patterns.
A brief that says a demographic \emph{naturally} or \emph{always} thinks something
is flagged.
\item \strong{Model} (optional, when \code{config} is a generative \code{LLMR::llm_config()}):
each brief is scored on caricature and essentialism on a 0--1 scale via
\code{\link[LLMR:llm_judge]{LLMR::llm_judge()}}. Without a config these scores are \code{NA}.
}

The lexical layer is a screening pass, not a proof: a clean scan does not certify a
brief is unbiased, and a hit may be a false positive in quoted speech. Treat
the audit as evidence to read, alongside the briefs themselves.
}
\examples{
set <- persona_variants(
  persona_frame("A small-business owner.", source = "synthetic"),
  vary = list(age = c("35", "60")))
persona_audit(set)
diagnostics(persona_audit(set))
\dontrun{
cfg <- LLMR::llm_config("openai", "gpt-4o-mini")
persona_audit(set, config = cfg)   # adds model caricature scores
}
}
\seealso{
\code{\link[=persona_frame]{persona_frame()}}, \code{\link[=persona_variants]{persona_variants()}}, \code{\link[=diagnostics]{diagnostics()}}
}
