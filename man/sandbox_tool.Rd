% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sandbox.R
\name{sandbox_tool}
\alias{sandbox_tool}
\title{Define a confined (sandboxed) tool}
\usage{
sandbox_tool(
  fn,
  name = NULL,
  description = NULL,
  parameters = NULL,
  required = NULL,
  mode = c("read_only", "tempdir", "container"),
  timeout_s = 30,
  max_bytes = 1e+06,
  allow_paths = NULL,
  env = "minimal",
  executor = NULL
)
}
\arguments{
\item{fn}{The function to expose, or an existing \code{llmr_tool} to confine. When
an \code{llmr_tool} is given, its own function, name, description, and schema
are reused and the remaining \code{name}/\code{description}/\code{parameters}/\code{required}
arguments are ignored.}

\item{name}{Tool name shown to the model. Defaults to \code{"sandboxed_tool"}.}

\item{description}{One or two sentences for the model. Defaults to
\code{"A sandboxed tool."}.}

\item{parameters}{A named list of JSON-Schema properties, or a full schema
object (as in \code{\link[LLMR:llm_tool]{LLMR::llm_tool()}}).}

\item{required}{Character vector of required argument names.}

\item{mode}{The confinement regime. \code{"read_only"}: the child runs with its
working directory set to a scratch directory; relative writes land there and
are hashed and checked against \code{allow_paths}, and any \emph{reported} write
outside \code{allow_paths} is a violation (the default executor cannot intercept
absolute-path writes; see Details). \code{"tempdir"}: the scratch directory is
the sanctioned writable location and is always permitted; reported writes
elsewhere are violations. \code{"container"}: confinement is delegated entirely to
a supplied \code{executor}, which is the way to obtain a hard filesystem boundary.}

\item{timeout_s}{Wall-clock limit per call, in seconds (default \code{30}). The
default executor kills the child process when it elapses; the call then
reports a \code{"timeout"} status.}

\item{max_bytes}{Maximum result size in bytes; a larger result (or captured
output) is truncated and flagged. Default \code{1e6}.}

\item{allow_paths}{Character vector of directories outside which a \emph{reported}
written file is a violation. The scratch working directory is always
permitted (in \code{"tempdir"} mode). The check applies only to files the
executor reports; with the default executor that means files written under
the scratch directory, since absolute-path writes elsewhere are not seen
(see Details). Default \code{NULL} (only the scratch directory is allowed).}

\item{env}{How much of the ambient environment the child sees. Recorded for
provenance and passed to the executor; the default executor treats
\code{"minimal"} as a hint and does not inherit the parent's global objects.}

\item{executor}{A function \verb{(fn, args, workdir, timeout_s)} implementing the
contract above. When \code{NULL}, a default child-process executor is built for
\code{"read_only"}/\code{"tempdir"} (requires \code{callr}; without it the function runs
in-process with a one-time warning that confinement is weak), and
\code{"container"} mode raises an error asking for an executor.}
}
\value{
An \code{llmr_tool} carrying a \code{"governance"} attribute whose \code{sandbox}
element records the mode and \code{allow_paths}. Each call returns the tool's
result string with a \code{"sandbox"} attribute recording the mode, duration,
byte count, input and output file hashes, and status.
}
\description{
Like \code{\link[LLMR:llm_tool]{LLMR::llm_tool()}}, but the tool runs under confinement: a wall-clock
timeout, a cap on result size, and auditing of where it writes. Each
invocation hashes the input files it was handed and the output files it
produced, so a tool that touches the filesystem leaves an auditable trail.
The returned object is an ordinary \code{llmr_tool}, so it passes to \code{\link[=agent]{agent()}}
and the tool loop exactly as a plain tool does.
}
\details{
Confinement is carried out by an \emph{executor}, a function that runs the user
function in a bounded place and reports what it produced. The default
executor runs the function in a child R process (via the \code{callr} package),
which is killed when the timeout elapses; \code{mode = "container"} requires an
executor to be supplied, because the package does not assume any particular
container runtime. Supplying \code{executor} directly is also how the tool is
tested offline: a fake executor returns a canned result and file list, so
the size, timeout, and path checks can be exercised without spawning
anything.

\strong{What the default executor actually guarantees.} The default child-process
executor runs the user function with its working directory set to a fresh
scratch directory, then snapshots that directory before and after the call to
hash every file written \emph{under it}. Relative writes therefore land in the
scratch directory and are hashed and checked against \code{allow_paths}. The
default executor does \strong{not} establish a hard filesystem boundary: an
absolute-path write outside the scratch directory (e.g. to \verb{/tmp/out} or
\verb{$HOME/out}) happens in the same operating-system namespace as the parent and
is \emph{not} intercepted, so \code{llmragent_sandbox_violation} cannot fire for a write
the executor never sees. The guarantee is therefore "confine and audit writes
\emph{within} the scratch working directory", not "block absolute-path writes". To
enforce a real boundary against arbitrary absolute paths, supply a
\code{mode = "container"} executor (or an OS-level sandbox) that runs the function
in a confined namespace and reports the files it wrote; the \code{allow_paths}
check then applies to whatever that executor reports.

The executor contract is \code{executor(fn, args, workdir, timeout_s)} returning
a list with elements \code{stdout} (character), \code{result} (the value, or \code{NULL}),
\code{files} (a named character vector mapping written paths to content hashes,
or a bare character vector of written paths), \code{status} (one of \code{"ok"},
\code{"timeout"}, \code{"error"}), and \code{error} (a message, or \code{NA}).
}
\examples{
\dontrun{
# A tool that runs in a killed-on-timeout child R process.
slow <- sandbox_tool(
  function(n) { Sys.sleep(n); "done" },
  name = "wait", description = "Sleeps for n seconds, then returns.",
  parameters = list(n = list(type = "number")),
  mode = "tempdir", timeout_s = 2
)
slow$fn(n = 10)  # reports a timeout rather than blocking

# Offline: an injected executor needs no process at all.
double <- sandbox_tool(
  function(x) x * 2, name = "double", description = "Doubles x.",
  parameters = list(x = list(type = "number")), mode = "tempdir",
  executor = function(fn, args, workdir, timeout_s)
    list(stdout = "", result = do.call(fn, args),
         files = character(0), status = "ok", error = NA)
)
double$fn(x = 21)
}
}
\seealso{
\code{\link[=agent_tool]{agent_tool()}}, \code{\link[LLMR:llm_tool]{LLMR::llm_tool()}}, \code{\link[=agent]{agent()}}
}
