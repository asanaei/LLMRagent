% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/society.R
\name{exposure_matrix}
\alias{exposure_matrix}
\title{Exposure matrix: who could see whom}
\usage{
exposure_matrix(society)
}
\arguments{
\item{society}{A \code{\link[=society]{society()}}.}
}
\value{
A numeric matrix (agents x agents) of 0/1 with agent ids as
dimnames; the diagonal is \code{0}.
}
\description{
Read the society's edge list as a symmetric agents-by-agents 0/1 matrix:
entry \verb{[i, j]} is \code{1} when agents \code{i} and \code{j} are connected (an edge exists
in either direction), \code{0} otherwise. Row and column names are agent ids. This
is the "who could see whom" structure the network encodes, separate from what
any given round actually showed each agent.
}
\examples{
\dontrun{
exposure_matrix(soc)
}
}
\seealso{
\code{\link[=society]{society()}}, \code{\link[=step_interaction]{step_interaction()}}
}
