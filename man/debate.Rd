% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/presets.R
\name{debate}
\alias{debate}
\title{Structured debate between two agents}
\usage{
debate(pro, con, topic, rounds = 2L, judge = NULL, quiet = FALSE, ...)
}
\arguments{
\item{pro, con}{\link{Agent}s arguing for and against.}

\item{topic}{The motion being debated.}

\item{rounds}{Number of rebuttal exchanges (default 2).}

\item{judge}{Optional \link{Agent}; if supplied, returns a verdict with a
winner, a confidence, and reasoning.}

\item{quiet}{Passed through; FALSE prints utterances live.}

\item{...}{Passed to the agents' underlying LLMR calls.}
}
\value{
An object of class \code{agent_debate}: a list with \code{transcript}
(tibble: \code{turn}, \code{phase}, \code{speaker}, \code{text}), \code{verdict} (list or NULL),
and \code{motion}. \code{as.data.frame()} returns the transcript.
}
\description{
Alternating statements in three phases: opening, \code{rounds} rebuttals each,
and closing. An optional judge then delivers a structured verdict. The
phase labels make it easy to analyze argument development over time.
}
\examples{
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0.7)
d <- debate(
  pro = agent("Pro", cfg, persona = "You argue FOR the motion, rigorously."),
  con = agent("Con", cfg, persona = "You argue AGAINST the motion, rigorously."),
  topic = "Social media does more harm than good to democratic discourse.",
  judge = agent("Judge", cfg, persona = "A strict, impartial debate judge.")
)
d$verdict
}
}
