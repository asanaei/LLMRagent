% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspector.R
\name{view_run}
\alias{view_run}
\title{View a run as a self-contained HTML inspector}
\usage{
view_run(run, output = NULL, open = interactive())
}
\arguments{
\item{run}{An \link{Agent} or any object accepted by \code{\link[=as_agent_run]{as_agent_run()}}.}

\item{output}{Path to write. Defaults to a temp file with extension \code{.html}.
A missing parent directory is created.}

\item{open}{If \code{TRUE} (the default in an interactive session) the file is
opened with \code{\link[utils:browseURL]{utils::browseURL()}} after writing.}
}
\value{
The output path, invisibly, with class \code{agent_inspector_path} (its
print method reports where the file was written).
}
\description{
\code{view_run()} renders an \link{agent_run} (or anything \code{\link[=as_agent_run]{as_agent_run()}} accepts)
into a single self-contained HTML file: a header (kind, run id, short
manifest hash, agents, creation time, total calls and tokens) followed by one
table per grain (utterance, event, call, tool). Event rows carry an HTML
anchor keyed by their \code{span_id}, and transcript and call cells that name a
span link to it, so a reader can trace an utterance to the model call, span
event, and tool output that produced it.
}
\details{
The inspector is a read-only view: it visualizes the substrate already captured by
the run and derives nothing. It prefers \code{htmltools} when that package is
installed; otherwise it builds the same content by hand with no optional
dependencies. If no HTML page can be produced at all, it writes a structured
text summary so the call still yields a file.
}
\examples{
\dontrun{
a <- agent("Ada", LLMR::llm_config("groq", "openai/gpt-oss-20b"))
a$chat("Tell me something true.")
view_run(a)
}
}
\seealso{
\code{\link[=as_agent_run]{as_agent_run()}}, \code{\link[=agent_manifest]{agent_manifest()}}, \code{\link[=report]{report()}}
}
