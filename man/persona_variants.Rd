% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persona.R
\name{persona_variants}
\alias{persona_variants}
\alias{print.persona_set}
\title{Vary a persona along named dimensions}
\usage{
persona_variants(p, vary, n = NULL, config = NULL)

\method{print}{persona_set}(x, ...)
}
\arguments{
\item{p}{A \code{\link[=persona_frame]{persona_frame()}} (the base).}

\item{vary}{A named list of the dimensions to vary. Enumerated mode reads the
level vectors (e.g. \code{list(age = c("28", "52"), risk = c("cautious", "tolerant"))}); generative mode reads only the names.}

\item{n}{Number of briefs to generate (generative mode only). Ignored when
enumerating.}

\item{config}{Optional generative \code{LLMR::llm_config()}. When \code{NULL}
(default), the set is enumerated offline.}

\item{x}{A \code{persona_set}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{persona_set}: a tibble with a \code{persona} list
column of \code{\link[=persona_frame]{persona_frame()}} objects, an \code{id} column (each frame's hash), a
\code{variant_of} column (the base hash), and one column per varied attribute.
}
\description{
Turn one \code{\link[=persona_frame]{persona_frame()}} into a designed \emph{set} of personas. Two modes:
}
\details{
\itemize{
\item \strong{Enumerated} (default, \code{config = NULL}): \code{vary} is a named list of level
vectors and the result is their full Cartesian product. Each combination is
rendered by appending the varied attributes to the base brief in plain,
factual language (no stereotyping copula), so the design is legible and the
base text is never rewritten.
\item \strong{Generative} (\code{config} is a generative \code{LLMR::llm_config()} and \code{n} is
given): one structured call asks the model for \code{n} individuated briefs that
vary \code{names(vary)}, under a hard-coded anti-essentialism instruction. Use
this when you want fluent, non-templated briefs; audit the result with
\code{\link[=persona_audit]{persona_audit()}}.
}

Varying a demographic attribute does not license writing a stereotype. The
enumerated renderer states attributes flatly; the generative prompt forbids
caricature. Neither mode certifies the output: it produces briefs to be
inspected, not a population to be trusted.
}
\examples{
base <- persona_frame("A first-time voter in a swing district.",
                      source = "synthetic")
set <- persona_variants(base, vary = list(age = c("19", "24"),
                                          leaning = c("undecided", "left")))
set
set$persona[[1]]$attributes
\dontrun{
cfg <- LLMR::llm_config("openai", "gpt-4o-mini")
gen <- persona_variants(base, vary = list(age = NA, occupation = NA),
                        n = 5, config = cfg)
persona_audit(gen)
}
}
\seealso{
\code{\link[=persona_frame]{persona_frame()}}, \code{\link[=persona_audit]{persona_audit()}}
}
