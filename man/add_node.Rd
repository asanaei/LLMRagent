% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{add_node}
\alias{add_node}
\title{Add a node to a workflow}
\usage{
add_node(
  wf,
  name,
  node,
  input_key = "input",
  output_key = NULL,
  schema = NULL,
  ...
)
}
\arguments{
\item{wf}{An \code{agent_workflow}.}

\item{name}{Node name (unique within the workflow).}

\item{node}{An \code{Agent}, a \verb{function(state)}, or a \code{human_gate()} marker.}

\item{input_key, output_key}{For an agent node: where to read the prompt and
write the reply in \code{state} (default \code{"input"} / the node name).}

\item{schema}{For an evaluator agent node: a JSON schema; the parsed verdict
is written to \code{state[[output_key]]}.}

\item{...}{Reserved.}
}
\value{
The workflow, with the node added. The first node added is the entry.
}
\description{
A node transforms the shared \code{state}. It may be: an \link{Agent} (its \code{reply()} is
called on \code{state[[input_key]]} and the result written to \code{state[[output_key]]});
a plain \verb{function(state)} returning the new state; an evaluator (a function or
\code{Agent} + \code{schema} writing a structured verdict into state, used by
conditional edges); or a \code{\link[=human_gate]{human_gate()}} (pauses the run for sign-off).
}
\seealso{
\code{\link[=agent_workflow]{agent_workflow()}}, \code{\link[=add_edge]{add_edge()}}
}
