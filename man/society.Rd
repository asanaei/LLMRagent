% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/society.R
\name{society}
\alias{society}
\alias{print.society}
\title{Assemble a society from a population, a network, and measures}
\usage{
society(population, network = NULL, measures = NULL)

\method{print}{society}(x, ...)
}
\arguments{
\item{population}{An \code{\link[=agent_population]{agent_population()}}.}

\item{network}{Who may interact with whom. \code{NULL} (default) means fully
connected. Otherwise a two-column edge list (a \code{data.frame} or \code{matrix})
of agent ids or integer indices, or an \code{igraph} graph (its edge list is
read via \code{igraph::as_edgelist()} when the package is installed).}

\item{measures}{Optional named list of \verb{function(agent) -> value}, applied by
\code{\link[=collect_measures]{collect_measures()}}.}

\item{x}{A \code{society}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{society}: a list with \code{population}, \code{edges} (a
tibble \code{from}, \code{to}), \code{measures}, \code{history} (a tibble \code{turn}, \code{step},
\code{speaker}, \code{text}), and \code{step} (the round counter, \code{0L} initially).
}
\description{
A \code{society} is the static apparatus an interaction simulation runs on: the
\code{\link[=agent_population]{agent_population()}} (its actors), an edge list (who may see whom), an
optional set of measurement functions, and an initially empty shared
transcript that \code{\link[=step_interaction]{step_interaction()}} grows one round at a time. It holds no
results until you step it.
}
\details{
The network constrains co-presence, not the engine. An edge between two
agents records that they are connected; \code{\link[=exposure_matrix]{exposure_matrix()}} reads the edges
as "who could see whom". The current stepping rule keeps the transcript
fully shared (every speaker sees the whole history) while still recording the
connectivity, so the exposure structure is available for analysis even
before a stricter visibility rule is added.
}
\examples{
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b")
pop <- agent_population(c("A.", "B.", "C."), config = cfg)
soc <- society(pop, network = data.frame(from = c("p1"), to = c("p2")))
soc <- step_interaction(soc, prompt = "Introduce yourself in one line.")
collect_measures(soc)
}
}
\seealso{
\code{\link[=step_interaction]{step_interaction()}}, \code{\link[=collect_measures]{collect_measures()}}, \code{\link[=exposure_matrix]{exposure_matrix()}},
\code{\link[=contamination_report]{contamination_report()}}
}
