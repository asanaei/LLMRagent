% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool_governance.R
\name{agent_tool}
\alias{agent_tool}
\title{Define a governed tool}
\usage{
agent_tool(
  fn,
  name,
  description,
  parameters = NULL,
  required = NULL,
  side_effects = c("read", "write", "external", "none"),
  requires_approval = FALSE,
  timeout_s = NULL,
  max_calls = Inf,
  max_bytes = Inf
)
}
\arguments{
\item{fn}{The R function to expose. Called with the model's arguments by
name, exactly as in \code{\link[LLMR:llm_tool]{LLMR::llm_tool()}}.}

\item{name}{Tool name shown to the model.}

\item{description}{One or two sentences for the model.}

\item{parameters}{A named list of JSON-Schema properties, or a full schema
object (as in \code{\link[LLMR:llm_tool]{LLMR::llm_tool()}}).}

\item{required}{Character vector of required argument names.}

\item{side_effects}{What the tool does in the world: \code{"read"} (default),
\code{"write"}, \code{"external"} (reaches a network or service), or \code{"none"}.}

\item{requires_approval}{If \code{TRUE}, each call pauses for human sign-off (see
\code{\link[=human_gate]{human_gate()}}); the agent then runs a controllable tool loop so the pause
is possible. Default \code{FALSE}.}

\item{timeout_s}{Optional wall-clock limit per call (seconds); needs the
\code{R.utils} package to enforce, otherwise it is recorded but not enforced.}

\item{max_calls}{Maximum times this \strong{tool object} may run (\code{Inf} by
default). A call beyond the limit is refused (the model is told, not the
tool executed). The counter lives in the tool object, so it is shared if
the same \code{agent_tool()} object is reused across agents or experiment cells;
build a fresh tool per independent cell (as you would a fresh agent) when
each cell should get its own budget.}

\item{max_bytes}{Maximum result size in bytes (as measured by
\code{nchar(type = "bytes")}); a larger result is truncated at a character
boundary within the cap and flagged.}
}
\value{
An \code{llmr_tool} carrying a \code{"governance"} attribute.
}
\description{
Like \code{\link[LLMR:llm_tool]{LLMR::llm_tool()}}, but the tool also declares how it behaves as a
research instrument: whether it reads, writes, or reaches outside the
session; whether a human must approve each call; and hard per-tool limits on
the number of calls, wall-clock time, and result size. The returned object is
an ordinary \code{llmr_tool}, so it passes to \code{\link[=agent]{agent()}} and the tool loop exactly
as a plain tool does; the governance is carried alongside and enforced on
every call.
}
\details{
Governance is provenance. A tool's policy is folded into the study manifest
via \code{\link[=hash_tool_spec]{hash_tool_spec()}}, so tightening a limit (a different apparatus) changes
the manifest hash. Each invocation is recorded with the hashes of its
arguments and result, its status, and its duration, surfaced at the \code{"tool"}
level of \code{\link[=as_agent_run]{as_agent_run()}}.
}
\examples{
lookup <- agent_tool(
  function(city) paste0("22C in ", city),
  name = "get_weather", description = "Current weather for a city.",
  parameters = list(city = list(type = "string")),
  side_effects = "external", max_calls = 5
)
}
\seealso{
\code{\link[LLMR:llm_tool]{LLMR::llm_tool()}}, \code{\link[=hash_tool_spec]{hash_tool_spec()}}, \code{\link[=guardrail]{guardrail()}}, \code{\link[=human_gate]{human_gate()}}
}
