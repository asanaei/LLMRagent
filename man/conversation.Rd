% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conversation.R
\name{conversation}
\alias{conversation}
\title{Run a multi-agent conversation}
\usage{
conversation(
  agents,
  topic,
  opening = NULL,
  opening_by = "Facilitator",
  turn_policy = c("round_robin", "random", "moderator"),
  moderator = NULL,
  max_turns = 2L * length(agents),
  stop_when = NULL,
  instruction = NULL,
  msg_mode = NULL,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{agents}{A list of \link{Agent} objects (names must be unique).}

\item{topic}{What the conversation is about; included in every speaker's
instructions.}

\item{opening}{Optional opening statement placed on the transcript before
the first turn (attributed to \code{opening_by}).}

\item{opening_by}{Name to attribute the opening to. Default "Facilitator".}

\item{turn_policy}{One of \code{"round_robin"}, \code{"random"}, \code{"moderator"}.}

\item{moderator}{An \link{Agent}; required for the moderator policy.}

\item{max_turns}{Total number of utterances to collect.}

\item{stop_when}{Optional \verb{function(transcript_tibble) -> logical}; checked
after every utterance.}

\item{instruction}{Extra instruction appended to every speaker's system
message (e.g. "Answer in at most three sentences.").}

\item{msg_mode}{Message construction for this run: \code{"roleflip"} (default) or
\code{"flat"}. \code{NULL} (the default) uses \code{getOption("LLMRagent.msg_mode")}, else
\code{"roleflip"}. An explicit value overrides the global option for this call
only. See the Message construction section.}

\item{quiet}{If FALSE (default), utterances print as they arrive.}

\item{...}{Passed to each agent's underlying LLMR call.}
}
\value{
An object of class \code{agent_conversation}: a list with \code{transcript}
(tibble: \code{turn}, \code{speaker}, \code{text}), \code{topic}, and \code{agents} (names).
}
\description{
Agents talk over a shared transcript. At each turn the next speaker (chosen
by \code{turn_policy}) receives the full dialogue so far, attributed by name,
plus an instruction to answer in character; the reply is appended and the
next turn begins. The conversation ends after \code{max_turns} utterances or
when \code{stop_when(transcript)} returns \code{TRUE}.
}
\details{
Turn policies:
\itemize{
\item \code{"round_robin"}: agents speak in the order given, repeatedly.
\item \code{"random"}: a random speaker each turn, never the same agent twice in a
row. Set a seed first (e.g. \code{set.seed(110)}) for a reproducible order.
\item \code{"moderator"}: after each utterance the \code{moderator} agent chooses who
speaks next (a structured one-token decision), which yields organic
dynamics at the cost of one extra cheap call per turn.
}

Replies are stateless (\link{Agent}'s \code{reply()}): the shared transcript is the
single source of truth, so the same agents can be reused across
conversations without cross-contamination.
}
\section{Message construction}{

By default each speaker sees the conversation role-flipped: its own prior
turns are \code{assistant} messages and every other speaker's are labeled \code{user}
messages (via \code{LLMR::transcript_as_messages()}), which gives the model a
structural handle on what it already said and reduces self-repetition.
\code{msg_mode = "flat"} (or \code{options(LLMRagent.msg_mode = "flat")}) reverts to the
legacy construction that pastes the whole attributed transcript into one
\code{user} message -- useful for reproducing pre-0.7.x runs. The same control
applies to the presets \code{\link[=debate]{debate()}}, \code{\link[=focus_group]{focus_group()}}, \code{\link[=interview]{interview()}}, and
\code{\link[=deliberate]{deliberate()}}.
}

\examples{
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0.8)
a <- agent("Rosa",  cfg, persona = "A pragmatic city planner.")
b <- agent("Hugo",  cfg, persona = "A skeptical economist.")
conv <- conversation(list(a, b),
                     topic = "Should the city pedestrianize its center?",
                     max_turns = 6)
conv$transcript
}
}
\seealso{
\code{\link[=debate]{debate()}}, \code{\link[=focus_group]{focus_group()}}, \code{\link[=interview]{interview()}}, \code{\link[=deliberate]{deliberate()}}
}
