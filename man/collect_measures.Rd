% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/society.R
\name{collect_measures}
\alias{collect_measures}
\title{Collect measures over a society}
\usage{
collect_measures(society, measures = NULL)
}
\arguments{
\item{society}{A \code{\link[=society]{society()}}.}

\item{measures}{Optional named list of \verb{function(agent) -> value}; defaults
to the society's own \code{measures}.}
}
\value{
A tibble with columns \code{agent_id}, \code{name}, \code{measure}, \code{value}, and
\code{step}, carrying attribute \code{uncalibrated = TRUE}.
}
\description{
Apply each measurement function to every agent and return one tidy row per
agent-by-measure, stamped with the society's current step. A measure that
errors yields \code{NA} for that agent rather than aborting the sweep. With no
measures, the default is each agent's utterance count in the shared history
(\code{"n_utterances"}).
}
\details{
The result always carries \code{attr(out, "uncalibrated") <- TRUE}. These are
model outputs, not measurements of people: the attribute is the structural
reminder that any quantity here is conditioned on the models and the prompts,
not validated against human data.
}
\examples{
\dontrun{
collect_measures(soc, measures = list(
  words = function(a) nchar(a$persona \%||\% "")))
}
}
\seealso{
\code{\link[=society]{society()}}, \code{\link[=step_interaction]{step_interaction()}}
}
