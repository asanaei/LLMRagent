% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness.R
\name{robustness-axes}
\alias{robustness-axes}
\alias{vary_models}
\alias{vary_temperature}
\alias{vary_prompt}
\alias{vary_persona}
\alias{vary_option_order}
\title{Robustness perturbation axes}
\usage{
vary_models(...)

vary_temperature(...)

vary_prompt(..., prompt = NULL, paraphrase = NULL, .config = NULL)

vary_persona(...)

vary_option_order(..., seed = 110)
}
\arguments{
\item{...}{For \code{vary_models}/\code{vary_temperature}, the levels (model names or
temperatures). For \code{vary_prompt}, either named template strings or
\code{paraphrase = n} with \verb{.config =} to generate \code{n} paraphrases. For
\code{vary_persona}, persona variants (strings or a \code{persona_set}). For
\code{vary_option_order}, the orders (\code{"as_is"}, \code{"reverse"}, \code{"random"}).}

\item{prompt}{For \code{vary_prompt(paraphrase=)}: the actual prompt text to
paraphrase. The paraphrase set is generated once at build time and is the
axis's levels (including the original as the baseline), so \code{run_fn}'s
\code{perturb$prompt(x)} returns the cell's paraphrase, not a placeholder.}

\item{paraphrase}{For \code{vary_prompt}: number of paraphrases to generate.}

\item{.config}{For \code{vary_prompt(paraphrase=)}: a generative config used once
to draft the paraphrases (hashed into the manifest).}

\item{seed}{For \code{vary_option_order}: RNG seed for the random permutation.}
}
\value{
An \code{agent_axis} object.
}
\description{
Helpers that declare a perturbation axis for \code{\link[=agent_robustness]{agent_robustness()}}. Each
returns a small spec the battery expands into design cells and applies to the
inputs through the \code{perturb} argument of your \code{run_fn}. Bare vectors work too
(\code{vary = list(model = c("a","b"))}).
}
\seealso{
\code{\link[=agent_robustness]{agent_robustness()}}
}
