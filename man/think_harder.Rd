% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/superbrain.R
\name{think_harder}
\alias{think_harder}
\title{Crack a hard problem with one strong model and many cheap ones}
\usage{
think_harder(
  problem,
  strong_config,
  cheap_config,
  n_approaches = 4L,
  verify = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{problem}{The problem statement (character scalar). Be complete:
workers see nothing but this and their assigned approach.}

\item{strong_config}{An \code{LLMR::llm_config()} for the strong (planner /
synthesizer / verifier) model.}

\item{cheap_config}{An \code{LLMR::llm_config()} for the cheap worker model.}

\item{n_approaches}{Number of independent lines of attack (default 4).}

\item{verify}{If TRUE (default), run the verification stage and one
revision round when the verifier finds a substantive flaw.}

\item{quiet}{FALSE prints stage progress.}

\item{...}{Passed to \code{LLMR::call_llm_par()} for the worker stage
(e.g. \code{tries}, \code{progress}).}
}
\value{
A list of class \code{super_brain}:
\describe{
\item{\code{answer}}{The final answer (character).}
\item{\code{plan}}{Tibble of approaches: \code{title}, \code{instructions}.}
\item{\code{workers}}{Tibble: \code{approach}, \code{output}, \code{success}, plus token
diagnostics from \code{LLMR::call_llm_par()}.}
\item{\code{verification}}{The verifier's structured critique (or NULL).}
\item{\code{revised}}{TRUE when the revision pass ran.}
}
}
\description{
A four-stage orchestration:
}
\details{
\enumerate{
\item \strong{Plan} (strong model, 1 call): decompose the problem into
\code{n_approaches} genuinely different lines of attack.
\item \strong{Work} (cheap model, \code{n_approaches} parallel calls): each line is
pursued independently, blind to the others.
\item \strong{Synthesize} (strong model, 1 call): weigh the drafts against one
another, resolve contradictions, and write the answer.
\item \strong{Verify} (strong model, 1 call, optional): attack the synthesized
answer; if a real flaw is found, one revision pass runs.
}

The economics: two to three strong-model calls regardless of how wide the
fan-out is, with the bulk of tokens billed at the cheap model's rate. The
returned object keeps every intermediate product, so you can audit what
each worker contributed and what the verifier objected to.
}
\examples{
\dontrun{
strong <- LLMR::llm_config("deepseek", "deepseek-reasoner")
cheap  <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0.8)
out <- think_harder(
  "A city of 1M residents wants to halve traffic deaths in 5 years
   with a budget of $40M. Propose the most cost-effective portfolio
   of interventions, with rough numbers.",
  strong_config = strong, cheap_config = cheap, n_approaches = 5
)
cat(out$answer)
out$workers[, c("approach", "success")]
}
}
