% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/society.R
\name{agent_population}
\alias{agent_population}
\alias{print.agent_population}
\title{Build a population of agents}
\usage{
agent_population(personas, n = NULL, config = NULL)

\method{print}{agent_population}(x, ...)
}
\arguments{
\item{personas}{Pre-built agents, a \code{persona_set}, a character vector of
briefs, or a single persona (frame or string) to replicate.}

\item{n}{Number of copies when \code{personas} is a single persona; ignored
otherwise.}

\item{config}{An \code{LLMR::llm_config()} used to build agents. Required unless
\code{personas} is already a list of \link{Agent}s.}

\item{x}{An \code{agent_population}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{agent_population}: a list with \code{agents} (a list
of \link{Agent}s), \code{ids} (their agent ids), and \code{n} (the count).
}
\description{
Assemble a list of \link{Agent}s from several kinds of input, the way an
agent-based study defines its actors. \code{agent_population()} accepts
several input forms and returns one strict output: whatever you pass,
you get back a flat list of constructed agents with stable ids.
}
\details{
\code{personas} may be:
\itemize{
\item a list of pre-built \link{Agent}s, used as is (no \code{config} needed);
\item a \code{\link[=persona_variants]{persona_variants()}} result (a \code{persona_set}), one agent per row, each
built from that row's \code{\link[=persona_frame]{persona_frame()}};
\item a character vector of persona briefs, one agent each;
\item a single \code{\link[=persona_frame]{persona_frame()}} or string with \code{n > 1}, replicated into \code{n}
agents named \verb{p1 ... pn}.
}

A population is scaffolding, not a sample: it inherits every limit of the
personas it is built from. Pair it with \code{\link[=persona_audit]{persona_audit()}} before reading any
result as if it spoke for the people the briefs sketch.
}
\examples{
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b")
pop <- agent_population(
  c("A cautious retiree.", "A risk-tolerant founder."), config = cfg)
pop
}
}
\seealso{
\code{\link[=society]{society()}}, \code{\link[=persona_variants]{persona_variants()}}, \code{\link[=agent]{agent()}}
}
