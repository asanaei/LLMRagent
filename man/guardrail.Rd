% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guardrails.R
\name{guardrail}
\alias{guardrail}
\title{Define a guardrail}
\usage{
guardrail(
  name,
  check,
  on_fail = c("block", "warn", "flag"),
  stage = c("input", "output", "tool")
)
}
\arguments{
\item{name}{A short label for the guardrail (appears in events).}

\item{check}{A function \verb{(payload, context) -> TRUE | reason}. \code{payload} is
the text (input/output) or a \code{list(name=, arguments=, result=)} (tool).
\code{context} is a small list with \code{stage}, \code{agent}, and \code{phase} (\code{"pre"} or
\code{"post"} for tools). Return \code{TRUE} to pass, or a non-empty character reason
to fail.}

\item{on_fail}{What to do on failure: \code{"block"} (raise
\code{llmragent_guardrail_block} and stop), \code{"warn"} (warn and continue), or
\code{"flag"} (record only). Default \code{"block"}.}

\item{stage}{Which boundary: \code{"input"}, \code{"output"}, or \code{"tool"}.}
}
\value{
An object of class \code{agent_guardrail}.
}
\description{
A guardrail is a named check run at one boundary of an agent: its input (the
user text), its output (the model's reply), or a tool call (a tool's name and
arguments before it runs, or its result after). The check returns \code{TRUE} to
pass, or a short reason string to fail. On failure the guardrail either
blocks (raises a typed condition and records the event), warns, or merely
flags; in every case the decision is recorded as an event, so a blocked input
is analyzable rather than invisible.
}
\examples{
no_pii <- guardrail(
  "no_ssn",
  function(payload, context) {
    if (grepl("\\\\b\\\\d{3}-\\\\d{2}-\\\\d{4}\\\\b", payload)) "contains an SSN" else TRUE
  },
  stage = "input"
)
}
\seealso{
\code{\link[=guardrails]{guardrails()}}, \code{\link[=agent]{agent()}}
}
