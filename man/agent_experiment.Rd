% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment.R
\name{agent_experiment}
\alias{agent_experiment}
\title{Run a factorial agent experiment}
\usage{
agent_experiment(design, run_fn, reps = 1L, parallel = FALSE, quiet = FALSE)
}
\arguments{
\item{design}{A data frame; each row is a condition, each column a factor
of the design (personas, prompts, treatments, model names, ...).}

\item{run_fn}{\verb{function(cond, rep)} where \code{cond} is one row of \code{design} as
a named list. Whatever it returns is stored in the \code{result} list-column.}

\item{reps}{Replications per condition (default 1).}

\item{parallel}{If TRUE, cells run concurrently via the \code{future} framework
(set a plan first, e.g. \code{future::plan(future::multisession)}).}

\item{quiet}{FALSE prints one progress line per completed cell.}
}
\value{
\code{design} expanded by replication, plus \code{rep}, \code{result}, \code{error},
and \code{duration} columns.
}
\description{
Takes a design frame (one row per condition), runs \code{run_fn} once per
condition and replication, and returns the design with \code{rep}, \code{result}
(list-column), \code{error}, and \code{duration} columns. A failing cell records its
error message and does not stop the others; re-run failures by filtering
on \code{!is.na(error)}.
}
\details{
\code{run_fn} receives the condition as a named list plus the replication
number, and should build its agents \emph{inside} the function so every cell
starts fresh:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{run_fn <- function(cond, rep) \{
  cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0.9)
  panel <- list(
    agent("A", cfg, persona = cond$persona_a),
    agent("B", cfg, persona = cond$persona_b)
  )
  deliberate(panel, cond$proposal, quiet = TRUE)
\}
}\if{html}{\out{</div>}}

A note on seeds: model replies are sampled server-side, so a local seed
never affects them. Set one only when your code draws random numbers
itself (a \code{run_fn} that randomizes stimuli, or the \code{"random"} turn policy
inside it); under \code{parallel = TRUE} the workers then use statistically
sound parallel streams (\code{future.seed}). Combine with
\code{LLMR::llm_log_enable()} to keep a per-call audit file of the whole
experiment.
}
\examples{
\dontrun{
design <- expand.grid(
  temperature = c(0.2, 1.0),
  framing = c("gains", "losses"),
  stringsAsFactors = FALSE
)
res <- agent_experiment(design, reps = 3, run_fn = function(cond, rep) {
  cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b",
                          temperature = cond$temperature)
  a <- agent("Subject", cfg, quiet = TRUE)
  a$reply(paste("Decide under", cond$framing, "framing: ..."))
})
}
}
\seealso{
\code{\link[=deliberate]{deliberate()}}, \code{\link[=debate]{debate()}}, \code{\link[LLMR:llm_usage]{LLMR::llm_usage()}}
}
