% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/society.R
\name{contamination_report}
\alias{contamination_report}
\title{Flag shared agent instances across a population}
\usage{
contamination_report(society)
}
\arguments{
\item{society}{A \code{\link[=society]{society()}}.}
}
\value{
An object of class \code{society_contamination}: a tibble with columns
\code{agent_id}, \code{name}, \code{slots} (the positions sharing that id), and \code{n} (how
many slots), one row per id that appears more than once. A \code{clean}
attribute records whether any duplicate was found.
}
\description{
A correct population is a set of distinct agents. If two slots hold the same
live \link{Agent} (built once and reused), they share memory, counters, and
identity, which silently couples the actors that should be independent. Each
Agent carries a stable id, so a duplicate id among the population's slots is
direct evidence of one reused instance. \code{contamination_report()} detects
exactly that.
}
\examples{
\dontrun{
contamination_report(soc)
}
}
\seealso{
\code{\link[=check_state_leakage]{check_state_leakage()}}, \code{\link[=agent_population]{agent_population()}}
}
