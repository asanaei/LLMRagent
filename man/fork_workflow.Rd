% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{fork_workflow}
\alias{fork_workflow}
\title{Fork a workflow run at a checkpoint}
\usage{
fork_workflow(
  x,
  wf,
  at = NULL,
  new_dir = NULL,
  mutate = NULL,
  max_steps = 64L,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{checkpoint_dir} or \code{agent_workflow_run}.}

\item{wf}{The \code{agent_workflow} (needed to run the branch).}

\item{at}{Step index to fork from (default: the last completed step).}

\item{new_dir}{Directory for the branch (default: a fresh temp dir).}

\item{mutate}{Optional \verb{function(state) -> state} applied before running.}

\item{max_steps, quiet, ...}{As in \code{\link[=run_workflow]{run_workflow()}}.}
}
\value{
An \code{agent_workflow_run} for the branch.
}
\description{
Copies the state at a chosen step into a new directory with a fresh run id,
optionally mutating it, and runs forward from there. The original run is
untouched: resume continues the same run, fork branches a new one.
}
\seealso{
\code{\link[=run_workflow]{run_workflow()}}, \code{\link[=resume_workflow]{resume_workflow()}}
}
