% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{replay_run}
\alias{replay_run}
\title{Replay a workflow run, verifying state hashes}
\usage{
replay_run(
  x,
  wf,
  verify = c("structural", "strict"),
  max_steps = 64L,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{checkpoint_dir} or \code{agent_workflow_run}.}

\item{wf}{The \code{agent_workflow}.}

\item{verify}{\code{"structural"} or \code{"strict"}.}

\item{max_steps, quiet, ...}{As in \code{\link[=run_workflow]{run_workflow()}}.}
}
\value{
An \code{agent_workflow_run} for the replay (its \code{steps} carries a
\code{replay_match} column).
}
\description{
Re-executes the graph from the original input and compares each node's
resulting state hash to the recorded one. \code{verify = "structural"} (default)
checks deterministic nodes (functions/evaluators) exactly and, for model
(agent) nodes, does not require the sampled text to match (model output is
nondeterministic); \code{verify = "strict"} requires every node to match (sound
only for fully deterministic graphs or archive-served calls). A mismatch
raises \code{llmragent_replay_mismatch} naming the first divergent node.
}
\seealso{
\code{\link[=run_workflow]{run_workflow()}}
}
