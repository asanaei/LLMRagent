% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{attach_calibration}
\alias{attach_calibration}
\title{Attach a calibration to an agent run}
\usage{
attach_calibration(run, cal)
}
\arguments{
\item{run}{An object accepted by \code{\link[=as_agent_run]{as_agent_run()}}.}

\item{cal}{An \code{agent_calibration} from \code{\link[=agent_calibrate]{agent_calibrate()}}.}
}
\value{
The modified \code{agent_run}.
}
\description{
Records a calibration on a run and folds its \code{manifest_patch} into the run's
design, so the run now carries its validated inference and its study
\code{\link[=agent_manifest]{agent_manifest()}} hash changes (the apparatus is no longer a bare
model-conditioned run; it is a calibrated estimate). The run's \code{claim_type}
is set to \code{"calibrated_inference"}, which suppresses the model-conditioned
caveat in \code{\link[=report]{report()}}.
}
\examples{
\dontrun{
run <- as_agent_run(a)
cal <- agent_calibrate(preds, gold = g, method = "ppi", estimand = "proportion")
run <- attach_calibration(run, cal)
}
}
\seealso{
\code{\link[=agent_calibrate]{agent_calibrate()}}, \code{\link[=agent_manifest]{agent_manifest()}}
}
