% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persistence.R
\name{save_agent}
\alias{save_agent}
\title{Save an agent to disk}
\usage{
save_agent(x, path)
}
\arguments{
\item{x}{An \link{Agent}.}

\item{path}{File path (\code{.rds}).}
}
\value{
\code{path}, invisibly.
}
\description{
Writes the agent's name, persona, config, memory contents, and trace to an
RDS file. Tools are functions and are not serialized; re-attach them at
load time via \code{load_agent(tools = ...)}.
}
\examples{
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b")
a <- agent("Ada", cfg, persona = "Terse.")
a$chat("Remember this: the project deadline is March 3.")
save_agent(a, "ada.rds")

# a later session, any machine with GROQ_API_KEY set:
ada <- load_agent("ada.rds")
ada$chat("When is the deadline?")   # the memory came along
}
}
\seealso{
\code{\link[=load_agent]{load_agent()}}
}
