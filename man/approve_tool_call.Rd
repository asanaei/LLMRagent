% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/approval.R
\name{approve_tool_call}
\alias{approve_tool_call}
\title{Approve, reject, or edit a pending tool call}
\usage{
approve_tool_call(
  checkpoint,
  decision = c("approve", "reject", "edit"),
  edit = NULL
)
}
\arguments{
\item{checkpoint}{A \code{llmragent_checkpoint}.}

\item{decision}{\code{"approve"} (run the tool as requested), \code{"reject"} (skip it;
the model is told the call was denied), or \code{"edit"} (run it with
\code{edit}-modified arguments).}

\item{edit}{For \code{decision = "edit"}, the replacement argument list.}
}
\value{
The checkpoint with the decision recorded.
}
\description{
When a run pauses at an approval-gated tool, it surfaces a checkpoint (the
\code{checkpoint} field of the \code{llmragent_pending_approval} condition, or the
object returned by \code{human_gate()} in batch mode). Inspect
\code{checkpoint$pending} (the tool name, arguments, and an argument hash), then
record a decision with this function and continue with \code{\link[=resume_run]{resume_run()}}.
}
\seealso{
\code{\link[=human_gate]{human_gate()}}, \code{\link[=resume_run]{resume_run()}}, \code{\link[=agent_tool]{agent_tool()}}
}
