% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\name{mcp_tools}
\alias{mcp_tools}
\title{Expose MCP server tools to an agent, under governance}
\usage{
mcp_tools(
  config,
  policy = c("read_only", "read_write"),
  approve_writes = TRUE,
  audit = TRUE,
  allow = NULL,
  pin_schemas = TRUE,
  transport = NULL,
  timeout_s = 30,
  max_bytes = 1e+06
)
}
\arguments{
\item{config}{A connection spec: a list with \code{url} (HTTP) or \code{command}
(stdio), or anything your \code{transport} understands.}

\item{policy}{\code{"read_only"} (default) or \code{"read_write"}.}

\item{approve_writes}{If \code{TRUE} (default), write/external calls pass a human
gate.}

\item{audit}{If \code{TRUE} (default), schemas/calls/results are recorded.}

\item{allow}{Optional character vector of tool names to expose (others are
dropped and logged).}

\item{pin_schemas}{If \code{TRUE} (default), pin and re-check tool schemas.}

\item{transport}{Test/extension seam: a function \verb{(method, params) -> result}
speaking JSON-RPC to the server. Default builds a real HTTP/stdio client.}

\item{timeout_s, max_bytes}{Per-call limits.}
}
\value{
A list of \code{llmr_tool} objects, each carrying MCP governance metadata.
}
\description{
Connects to a Model Context Protocol server and returns its tools as
\code{\link[LLMR:llm_tool]{LLMR::llm_tool()}} objects ready for \code{\link[=agent]{agent()}}, wrapped so the agent's use of
them is safe and recorded. The defaults are strict
because MCP's documented attack surface (tool/schema poisoning, line jumping,
rug pulls, confused-deputy) lives in exactly the trust an agent places in a
server's tool descriptions.
}
\details{
Defenses applied:
\itemize{
\item \strong{Read-only floor} (\code{policy = "read_only"}): the floor is an allowlist, not
a denylist. A tool is exposed only when it is \emph{positively} known to be
read-only (its \code{annotations$readOnlyHint} is \code{TRUE}). Any tool that is not
positively read-only (one with no annotations, or one that looks
write-like) is refused unless \code{policy = "read_write"}. A malicious server
cannot slip a writing tool past the floor by giving it a benign name and no
annotations.
\item \strong{Human gate for writes} (\code{approve_writes = TRUE}): any write/external call
pauses for sign-off (see \code{\link[=human_gate]{human_gate()}}).
\item \strong{Schema pinning} (\code{pin_schemas = TRUE}): each tool's full advertised
signature (input schema, description, and annotations) is hashed at first
listing and re-verified before every call. A later change, or a
server that refuses to let us re-verify (a \code{tools/get} that errors or
returns no schema), raises \code{llmragent_mcp_schema_drift} rather than
trusting the new definition or failing open (the schema-drift defense). The
re-check \emph{fails closed}: if it cannot confirm the tool is unchanged, it
refuses.
\item \strong{Description sanitation}: server descriptions are treated as untrusted,
never spliced into a system prompt, and scanned for injection patterns;
a flagged tool is downgraded to require approval (the line-jumping defense).
\item \strong{Audit} (\code{audit = TRUE}): tool schemas, call argument hashes, and result
hashes are recorded.
}
}
\examples{
\dontrun{
# offline, via a fake transport returning canned JSON-RPC
fake <- function(method, params) {
  if (method == "tools/list") list(tools = list(list(
    name = "search", description = "Search docs.",
    inputSchema = list(type = "object",
                       properties = list(q = list(type = "string"))))))
  else list(content = list(list(type = "text", text = "result")))
}
tools <- mcp_tools(list(url = "http://localhost:9000"), transport = fake)
}
}
\seealso{
\code{\link[=agent]{agent()}}, \code{\link[=agent_tool]{agent_tool()}}, \code{\link[=human_gate]{human_gate()}}
}
