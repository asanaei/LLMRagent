% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness.R
\name{agent_robustness}
\alias{agent_robustness}
\title{Run a robustness battery}
\usage{
agent_robustness(
  run_fn,
  design = NULL,
  reps = 1L,
  vary = list(),
  measure = NULL,
  baseline = "first",
  config = NULL,
  parallel = FALSE,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{run_fn}{The procedure, \verb{function(cond, rep[, perturb])}, returning a
result whose stability is assessed via \code{measure}.}

\item{design}{Optional baseline conditions (a data frame); one empty
condition if \code{NULL}.}

\item{reps}{Replications per cell.}

\item{vary}{A named list of axes: bare level vectors or axis specs from
\code{\link[=vary_models]{vary_models()}} and related helpers.}

\item{measure}{A function \code{result -> scalar} (or a field name) producing the
quantity whose stability is assessed. If \code{NULL}, the result is used when it
is already scalar.}

\item{baseline}{Which level of each axis is the reference (\code{"first"}).}

\item{config}{A base config (used to build \code{perturb$config}).}

\item{parallel}{Passed to \code{\link[=agent_experiment]{agent_experiment()}}.}

\item{quiet}{Passed to \code{\link[=agent_experiment]{agent_experiment()}}.}

\item{...}{Passed to \code{\link[=agent_experiment]{agent_experiment()}}.}
}
\value{
An object of class \code{agent_robustness}: a list with \code{cells} (the full
perturbed design with \code{measure_value}), \code{by_axis} (one row per axis-level
with \code{instability}, \code{dispersion}, \code{agreement_alpha}, \code{failure_rate},
\code{flips_vs_baseline}, \code{delta_mean}), and \code{overall} (a \code{fragile} flag).
}
\description{
Runs a procedure across the cross-product of perturbation axes and reports,
by axis, how much the result moves. The procedure is your \code{run_fn}; the
perturbations reach it through an optional third argument, \code{perturb}, so you
write the procedure once and the battery varies its inputs.
}
\details{
\code{run_fn} may be \verb{function(cond, rep)} (the existing \code{\link[=agent_experiment]{agent_experiment()}}
contract) or \verb{function(cond, rep, perturb)}. \code{perturb} is a list with
\code{config} (the base config with this cell's \code{model}/\code{temperature} applied),
\code{persona(x)} (apply this cell's persona variant to a base persona), \code{prompt(x)}
(apply this cell's prompt variant), and \code{reorder(options)} (apply this cell's
option permutation). A two-argument \code{run_fn} still runs; then only the
\code{model}/\code{temperature} axes affect the run.
}
\examples{
\dontrun{
batt <- agent_robustness(
  run_fn = function(cond, rep, perturb) {
    a <- agent("S", perturb$config, persona = perturb$persona("A cautious voter."))
    a$reply(perturb$prompt("Do you support the policy? yes/no."))
  },
  vary = list(temperature = c(0, 1), model = c("openai/gpt-oss-20b")),
  measure = function(r) tolower(trimws(r))
)
batt$by_axis
}
}
\seealso{
\code{\link[=agent_experiment]{agent_experiment()}}, \code{\link[LLMR:llm_replicate]{LLMR::llm_replicate()}}, \code{\link[LLMR:llm_agreement]{LLMR::llm_agreement()}}
}
