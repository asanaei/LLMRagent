% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{agent_pipeline}
\alias{agent_pipeline}
\title{Run input through a chain of agents}
\usage{
agent_pipeline(agents, input, quiet = FALSE, ...)
}
\arguments{
\item{agents}{A list of \link{Agent}s, in order. A single agent is allowed.}

\item{input}{The text handed to the first agent.}

\item{quiet}{If FALSE (default), each stage's output prints as it arrives.}

\item{...}{Passed to each agent's underlying LLMR call.}
}
\value{
An object of class \code{agent_pipeline_run}: a list with \code{steps}
(tibble: \code{step}, \code{agent}, \code{input}, \code{output}) and \code{output} (the final
text). \code{as.data.frame()} returns the steps.
}
\description{
Each agent receives the previous agent's output as its message (the first
receives \code{input}), transforms it according to its persona, and hands the
result on. The classic use is an assembly line of narrow specialists --
extract, then translate, then critique -- where each stage is easy to
inspect, test, and swap.
}
\details{
Stages are stateless (\code{reply()}), so the same agents can serve in several
pipelines, and a pipeline can run many inputs without cross-contamination.
Every intermediate product is kept: the returned \code{steps} tibble has one
row per stage with the exact input and output of each agent.
}
\examples{
\dontrun{
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0.3)

run <- agent_pipeline(
  list(
    agent("Extractor", cfg, persona =
      "Extract every factual claim as a numbered list. Nothing else."),
    agent("Checker", cfg, persona =
      "For each numbered claim, mark VERIFIABLE or VAGUE, one line each."),
    agent("Editor", cfg, persona =
      "Rewrite the original message keeping only VERIFIABLE claims.")
  ),
  input = "Our app doubled retention, won three awards, and users love it."
)
run$output      # the final text
run$steps       # every intermediate product
}
}
\seealso{
\code{\link[=agent_as_tool]{agent_as_tool()}} for model-directed (rather than fixed) routing.
}
