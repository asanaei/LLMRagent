% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent_run.R
\name{as_agent_run}
\alias{as_agent_run}
\alias{agent_run}
\alias{as_tibble.agent_run}
\title{Convert an LLMRagent result to a unified run object}
\usage{
as_agent_run(x, ...)

\method{as_tibble}{agent_run}(x, ..., level = c("utterance", "event", "call", "tool", "state"))
}
\arguments{
\item{x}{An \link{Agent}; a conversation, debate, focus group, interview, or
deliberation; an \code{agent_pipeline_run}; an \code{agent_fanout_result}; an
\code{agent_experiment};
or a \code{\link[LLMR:call_llm_par]{LLMR::call_llm_par()}}-style result frame.}

\item{...}{Unused.}

\item{level}{The grain to return: \code{"utterance"} (analysis grain),
\code{"event"} (every span), \code{"call"} (one canonical \code{\link[LLMR:llm_response_record]{LLMR::llm_response_record()}}
row per model call), \code{"tool"} (tool invocations with arg/result hashes), or
\code{"state"} (each participating agent's memory at run end).}
}
\value{
An object of class \code{agent_run}.
}
\description{
\code{as_agent_run()} turns any high-level result into an \code{agent_run}: a single
object that exposes a run at five levels through one tidy accessor,
\code{tibble::as_tibble(run, level = c("utterance","event","call","tool","state"))},
and that backs \code{\link[=agent_manifest]{agent_manifest()}}, \code{\link[=archive_agent_study]{archive_agent_study()}},
\code{\link[=diagnostics]{diagnostics()}}, and \code{\link[=report]{report()}}.
}
\details{
Provenance is captured during every model call, so
converting costs nothing during the run; the views are built on demand. For a
bare \link{Agent}, the result is a live view of the agent's session so far.
}
\seealso{
\code{\link[=agent_manifest]{agent_manifest()}}, \code{\link[=archive_agent_study]{archive_agent_study()}}, \code{\link[=diagnostics]{diagnostics()}},
\code{\link[=report]{report()}}
}
