% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_llmr.R
\name{report.agent_run}
\alias{report.agent_run}
\title{Draft a methods-section report for an agent run}
\usage{
\method{report}{agent_run}(x, ...)
}
\arguments{
\item{x}{An object accepted by \code{\link[=as_agent_run]{as_agent_run()}}.}

\item{...}{May include \code{task}, a one-clause description of what the model was
asked to do (spliced into the methods paragraph).}
}
\value{
An object of class \code{agent_report}: a character vector with a print
method that \code{cat()}s the lines.
}
\description{
Composes a short, citable account of a run: a design header (kind, run id,
the participating agents, and a compact workflow summary), the model and
token paragraph drafted by \code{\link[LLMR:llm_methods_text]{LLMR::llm_methods_text()}} over the run's
call-level rows, and a one-line limits note. When no calibration is attached
and the run is not a calibrated inference, the note states that the results
are model-conditioned and are not estimates of a human population.
}
\examples{
\dontrun{
a <- agent("Aria", LLMR::llm_config("groq", "openai/gpt-oss-20b"))
a$chat("Hello")
report(a, task = "to answer a factual question")
}
}
\seealso{
\code{\link[=diagnostics]{diagnostics()}}, \code{\link[LLMR:llm_methods_text]{LLMR::llm_methods_text()}}
}
