% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leakage.R
\name{check_state_leakage}
\alias{check_state_leakage}
\title{Detect shared state across experiment cells}
\usage{
check_state_leakage(x)
}
\arguments{
\item{x}{An \code{\link[=agent_experiment]{agent_experiment()}} result (the tibble with a \code{result}
list-column), or a plain list whose elements are \link{Agent}s or \code{agent_run}
objects (each element treated as one cell).}
}
\value{
An object of class \code{agent_leakage_report}: a list with \code{leaks} (a
tibble with columns \code{cell_i}, \code{cell_j}, \code{agent_id}, \code{kind}, \code{evidence}),
\code{clean} (\code{TRUE} when no leaks were found), and \code{n_cells}.
}
\description{
\code{check_state_leakage()} inspects the cells of an \code{\link[=agent_experiment]{agent_experiment()}} (or a
plain list of convertible results) and reports whether any cell shares a live
agent with another. A correct experiment builds its agents \emph{inside}
\code{run_fn}, so each cell gets fresh agents with distinct ids; an agent built
once \emph{outside} \code{run_fn} and reused leaks its memory and identity across
cells, which silently couples conditions that should be independent.
}
\details{
The check is read-only and conservative: every cell is converted through
\code{\link[=as_agent_run]{as_agent_run()}} inside a \code{tryCatch()}, and a cell whose result is not
run-able (a number, a string, anything off the provenance path) is skipped
rather than treated as evidence. Two kinds of leak are reported:
\itemize{
\item \code{shared_agent_instance}: one \code{agent_id} participates in two different
cells. Distinct cells with a fresh agent each cannot collide on an id, so
a shared id is direct evidence of one reused instance.
\item \code{memory_bleed}: a stronger signal on top of a shared id, raised when the
later cell's agent memory already contains content hashed from the earlier
cell, i.e. the agent literally remembers the prior condition.
}
}
\examples{
\dontrun{
design <- data.frame(framing = c("gains", "losses"))

# leaks: one agent is built once and reused by every cell
shared <- agent("S", LLMR::llm_config("groq", "openai/gpt-oss-20b"))
bad <- agent_experiment(design, reps = 1, run_fn = function(cond, rep) {
  shared$chat(cond$framing); shared
})
check_state_leakage(bad)          # clean = FALSE

# clean: a fresh agent is built inside every cell
good <- agent_experiment(design, reps = 1, run_fn = function(cond, rep) {
  a <- agent("S", LLMR::llm_config("groq", "openai/gpt-oss-20b"))
  a$chat(cond$framing); a
})
check_state_leakage(good)         # clean = TRUE
}
}
\seealso{
\code{\link[=agent_experiment]{agent_experiment()}}, \code{\link[=as_agent_run]{as_agent_run()}}
}
