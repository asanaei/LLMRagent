% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive.R
\name{archive_agent_study}
\alias{archive_agent_study}
\title{Seal an agent study to a directory}
\usage{
archive_agent_study(
  run,
  path,
  include_messages = TRUE,
  redact = NULL,
  formats = c("csv", "jsonl", "rds")
)
}
\arguments{
\item{run}{An object accepted by \code{\link[=as_agent_run]{as_agent_run()}} (an \link{Agent}, a conversation
or preset result, a pipeline, an experiment, or an \code{agent_run}).}

\item{path}{Directory to write into; created (recursively) if absent.}

\item{include_messages}{If \code{TRUE} (default), write the transcript and the
free-text columns of the tool and state tables. If \code{FALSE}, those tables
are still written but their free-text columns are blanked, keeping only
structure, hashes, and metadata.}

\item{redact}{Optional redaction applied to the free-text columns of the
written transcript, tool, and state tables (\code{text}, \code{content}, \code{arguments},
\code{result}, \code{response_text}) -- never to hashes, and never to the verbatim
audit log. Either a \verb{function(text) -> text}, or a character vector of
regular expressions, each of which is replaced by \code{"[REDACTED]"}. Redaction
is applied to the on-disk copy after hashing, so the join invariant holds.}

\item{formats}{Which optional formats to write, any of \code{"csv"} (the tabular
views), \code{"jsonl"} (events and calls; always written), and \code{"rds"} (the
\code{agent_run} object as \code{run.rds}). Defaults to all three.}
}
\value{
Invisibly, an object of class \code{agent_archive}: a list with \code{path},
\code{files} (relative paths written), \code{manifest_hash}, and \code{n_calls}. Print
lists what was written and confirms the seal.
}
\description{
Writes a self-contained, hash-sealed archive of a run: the study
\code{\link[=agent_manifest]{agent_manifest()}} (\code{manifest.json}), the transcript (\code{transcript.csv}), the
event / call / tool / state views, the per-call provenance (\code{calls.jsonl},
the LLMR audit log copied verbatim when one was kept), any artifacts, a
drafted methods note (\code{README-methods.md}), and a \code{hashes.sha256} manifest
over every file written. The archive is the supplementary material a paper
can ship: it carries the apparatus's identity and the calls' provenance, not
just the prose.
}
\details{
Hashes are identity, not outcome. The \code{request_hash} in \code{calls.jsonl} is
computed over the original request, so a call read back from the archive
still matches the same call issued live. Redaction therefore never touches a
hash, and when an LLMR audit log is present it is copied byte-for-byte rather
than reserialized.
}
\examples{
\dontrun{
a <- agent("Aria", LLMR::llm_config("groq", "openai/gpt-oss-20b"))
a$chat("Hello")
archive_agent_study(a, tempfile("study_"))
}
}
\seealso{
\code{\link[=agent_manifest]{agent_manifest()}}, \code{\link[=as_agent_run]{as_agent_run()}}, \code{\link[LLMR:llm_log_read]{LLMR::llm_log_read()}}
}
